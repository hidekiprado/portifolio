import { render, screen, fireEvent } from "@testing-library/react";
import Home from "./Home";

test("first test", () => {});

// test("Title Home/h1 has to be on the screen", () => {
//   render(<Home />);
//   const h1Title = screen.getByText("Home");
//   expect(h1Title).toBeInTheDocument();
// });
